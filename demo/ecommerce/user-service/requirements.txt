fastapi==0.115.0
uvicorn[standard]==0.30.6
pymysql==1.1.1
# MySQL 8 defaults to caching_sha2_password; PyMySQL can only negotiate that
# auth plugin when cryptography is installed. Without it, every connection
# fails at handshake with "'cryptography' package is required".
cryptography==43.0.1
sqlalchemy==2.0.35
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 misreads bcrypt>=4.1 version metadata
pyjwt==2.9.0
pydantic[email]==2.9.2
prometheus-client==0.21.0
python-json-logger==2.0.7
# OpenTelemetry (traces). Export is a no-op if OTEL_EXPORTER_OTLP_ENDPOINT is unset.
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
# Required: opentelemetry-instrumentation 0.48b0 imports pkg_resources, and
# python:3.12-slim no longer bundles setuptools. See order-service/requirements.txt.
setuptools==75.1.0